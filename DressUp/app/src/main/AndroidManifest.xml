<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mvc.dresssup">
    <!--<uses-feature android:name="android.hardware.camera" />-->
    <!--<uses-feature android:name="android.hardware.camera.autofocus" />-->
    <!--<uses-feature android:glEsVersion="0x00020000" android:required="true" />-->

    <!--<uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />-->
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <!--<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />-->
    <!-- 用于读取手机当前的状态 -->
    <!--<uses-permission android:name="android.permission.READ_PHONE_STATE" />-->
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <!--<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />-->
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 听筒和扬声器模式切换 -->
    <!--<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>-->

    <!-- 音视频 -->
    <!--<uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />-->


    <!-- GCM required, remember replace "com.easemob.helpdeskdemo" to your own package -->
    <!--<uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />-->

    <!--<permission-->
        <!--android:name="com.mvc.dresssup.permission.C2D_MESSAGE"-->
        <!--android:protectionLevel="signature" />-->

    <!--<uses-permission android:name="com.mvc.dresssup.permission.C2D_MESSAGE" />-->

    <!-- 其中的com.easemob.helpdeskdemo需要换为自己的包名 -->
    <!--<permission-->
        <!--android:name="com.mvc.dresssup.permission.MIPUSH_RECEIVE"-->
        <!--android:protectionLevel="signatureOrSystem" />-->
    <!-- 其中的com.easemob.helpdeskdemo需要换为自己的包名 -->
    <!--<uses-permission android:name="com.mvc.dresssup.permission.MIPUSH_RECEIVE" />-->
    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LocationActivity" />
        <!-- <activity android:name=".SearchActivity" /> -->
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.ClassificationActivity" />
        <activity android:name=".activity.AddNewLocationActivity" />
        <activity android:name=".activity.PopularbrandsActivity" />
        <activity android:name=".activity.ProductDetailActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".activity.MakeSureOrderActivity" />
        <activity android:name=".activity.LocationChooseActivity" />
        <activity android:name=".activity.OrderManagerActivity" />
        <activity android:name=".activity.PayOrderActivity" />
        <activity android:name=".activity.OrderDetailActivity" />
        <activity android:name=".activity.SettingActivity"></activity>


        <!-- 聊天页面 -->
        <activity
            android:name=".chat.ChatActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|navigation"
            android:theme="@style/horizontal_slide"
            android:windowSoftInputMode="adjustResize|stateHidden"/>

    </application>

</manifest>